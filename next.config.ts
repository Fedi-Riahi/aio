import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','plus.unsplash.com','images.pexels.com','assets.aceternity.com','tunis.events','media.pathe.tn'], 
  },
};

export default nextConfig;
